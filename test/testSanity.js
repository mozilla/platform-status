var expect = require('chai').expect;

describe('SanityCheck', function() {
  this.timeout(1000);

  before(function() {
  });

  it('should simply pass', function() {
    expect(true);
  });

  after(function () {
  });
});
